name: demo-workflow

on: 
  push:

jobs:
  base_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install cowsay
        run: |
          sudo apt-get update
          sudo apt-get install -y cowsay

      - name: Dragon says Hello
        run: |
          MESSAGE="Deploy successful! 🐉"
          echo "Generating a dragon message..."
          cowsay -f dragon "$MESSAGE"

      - name: test
        run: |
          echo "Running test..."
          echo "Hello"

      - name: list
        run: |
          echo "Listing files..."
          ls
