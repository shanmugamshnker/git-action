name: demo-workflow

on: 
  push:

jobs:
  base_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: test
        run: echo "Hello"

      - name: list
        run: ls
      - name: check the file content
        run: cat README.md
